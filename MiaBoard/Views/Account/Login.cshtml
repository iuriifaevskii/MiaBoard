@model MiaBoard.Models.LoginViewModel
@{
    ViewBag.Title = "Log in";
}
<style>
    body {
        background: #298eea;
    }
</style>
<!-- Login -->
<div class="container-fluid font-login ">
        <div class="col-xs-12 col-sm-6 col-md-4 col-sm-offset-3 col-md-offset-4">
            <div class="panel panel-default" id="Login-Panel">
                <div class="panel-body text-center">
                    <h4 class="text-center" id="log-in">Log In</h4>
                    <section id="loginForm">
                        @using (Html.BeginForm("Login", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
                        {
                            @Html.AntiForgeryToken()
                            <div class="validation-summary-errors" data-valmsg-summary="true">
                                <div class="text-danger">
                                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                                    @ViewBag.InvalidLogin
                                </div>
                            </div>
                            <div class="form-group">
                                <div class="col-md-10 col-md-offset-1">
                                    @Html.TextBoxFor(m => m.Login, new { @class = "form-control", placeholder = "Email" })
                                    @Html.ValidationMessageFor(m => m.Login, "", new { @class = "text-danger" })
                                </div>
                            </div>
                            <div class="form-group">
                                <div class="col-md-10 col-md-offset-1">
                                    @Html.PasswordFor(m => m.Password, new { @class = "form-control", placeholder = "Password" })
                                    @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger" })
                                </div>
                            </div>
                            <div class="form-group">
                                <div class="col-md-10 col-md-offset-1">
                                    <div style="text-align:left" class="checkbox">
                                        @Html.CheckBoxFor(m => m.IsRememberMe)
                                        @Html.LabelFor(m => m.IsRememberMe)
                                        @Html.ValidationMessageFor(model => model.IsRememberMe, "", new { @class = "text-danger" })
                                    </div>
                                </div>
                            </div>
                            <div class="form-group">
                                <div class="col-md-6 col-md-offset-3 text-center">
                                    <input id="Login-Btn" type="submit" class="btn btn-default btn-rounded waves-effect waves-light btn-block" value="Login">
                                </div>
                            </div>
                        }
                    </section>
                </div>
            </div>
            <p id="No-Account" class="center-block text-center">
                Don't have an account?
                @Html.ActionLink("Register", "Register", "Account",
                new
                {
                    id = "Sign-up"
                }
            )
            </p>
        </div>
    </div>
<!-- End Login -->
@* Enable this once you have account confirmation enabled for password reset functionality
    <p>
        @Html.ActionLink("Forgot your password?", "ForgotPassword")
    </p>
*@


@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
